-- HireNest Database Schema (reference)
-- Tables are auto-created by Hibernate (spring.jpa.hibernate.ddl-auto=update)

CREATE DATABASE IF NOT EXISTS hirenest;
USE hirenest;

-- Core tables: users, applicants, recruiters, jobs, applications,
-- saved_jobs, messages, notifications, applicant_skills, job_skills
